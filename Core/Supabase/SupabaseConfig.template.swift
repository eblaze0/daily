import Foundation

// IMPORTANT: Copy this file to SupabaseConfig.swift and add your credentials
// SupabaseConfig.swift is ignored by Git to keep your credentials safe

enum SupabaseConfig {
    // Add your Supabase credentials here
    static let supabaseURL = "YOUR_SUPABASE_URL"
    static let supabaseKey = "YOUR_SUPABASE_ANON_KEY"
} 